# Umbra Environment Variables
# ============================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# All variables are optional — sensible defaults are used when unset.

# ── Relay Server ──────────────────────────────────────────────────────────────
# Override the default relay URL (defaults to https://relay.umbra.chat)
# EXPO_PUBLIC_RELAY_URL=https://relay.umbra.chat

# ── TURN Server ───────────────────────────────────────────────────────────────
# Shared secret for generating TURN credentials (for voice/video calls).
# If unset, credentials are fetched from the relay's /api/turn endpoint.
# EXPO_PUBLIC_TURN_SECRET=

# ── Google Fonts ──────────────────────────────────────────────────────────────
# API key for Google Fonts (community customization). Optional.
# Get one at: https://developers.google.com/fonts/docs/developer_api
# EXPO_PUBLIC_GOOGLE_FONTS_API_KEY=
